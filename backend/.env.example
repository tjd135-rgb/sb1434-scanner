# Copy to .env for local dev. Render sets DATABASE_URL from render.yaml.
DATABASE_URL=postgresql://sb1434:sb1434@localhost:5432/sb1434

# Comma-separated origins. Not currently enforced (CORS is wildcarded) but
# reserved for future auth surface.
CORS_ORIGINS=http://localhost:3000
